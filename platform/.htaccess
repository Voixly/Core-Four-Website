<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger serves this folder. The Laravel app is in public/.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
